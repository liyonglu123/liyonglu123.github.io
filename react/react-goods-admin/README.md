### react-good-admin 后台管理
```
    1. git node 环境的搭建
    2. webpack 基本搭建 html单独打包
    3. babel  == 注意版本的问题
    4. react react-dom
    5. CSS 加载  style-loader css-loader
    6. css 单独打包
    7. scss  的配置 == 需要安装node-sass  注意版本的为问题
    8. 图片资源加载  url-loader file-loader
    9. 字体图标加资 同上
    10. 路径的优化以及   图片的name: 'resource/[name].[ext]' 的配置
    11. webpack-dev-server  自动刷新  Cannot find module 'webpack-cli/bin/config-yargs  和webpack的版本问题
    12. package.json 中script脚本的配置
```
