### typescript的学习
```
    1. 项目的初始化
    2. npm install typescript -g  之后  tsc --init  生成tsconfig.json lib:[]的作用
    3. npm install typescript  安装的问题 初始化tsconfig.json
    4. npm install tslint -g ==== tslint -i  初始化配置 tslint.json
    5. 安装webpack  npm install webpack webpack-cli webpack-dev-server -D   
    6. npm install cross-env  区分是生产还是开发环境的
    7. 配置weback.config.js 
```